<div class="min-h-screen flex items-center justify-center -mt-6">
  <div class="bg-white rounded-2xl shadow-lg p-10 w-full max-w-md">
    <div class="text-center mb-8">
      <span class="text-5xl">🏢</span>
      <h1 class="text-2xl font-bold text-gray-800 mt-3">Nouveau service</h1>
    </div>

    <%= render "form", service: @service, submit_label: "Créer le service" %>
  </div>
</div>
